{
  "name": "projeto-agente",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:pwa": "vite build && playwright test --config=playwright.pwa.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "sharp": "^0.35.3",
    "vite": "^8.1.1",
    "vite-plugin-pwa": "^1.3.0"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "fuse.js": "^7.5.0"
  }
}
